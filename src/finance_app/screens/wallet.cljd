(ns finance-app.screens.wallet
  (:require
   ["package:flutter/material.dart" :as m]
   [cljd.flutter.alpha2 :as f]))

(def screen
  (f/widget
   :get [m/Navigator]
   :context ctx
   (m/Scaffold)
   .body
   (m/Center
    .child
    (m/Text "wallet"))))
