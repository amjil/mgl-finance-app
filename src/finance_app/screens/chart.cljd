(ns finance-app.screens.chart
  (:require
   ["package:flutter/material.dart" :as m]
   [cljd.flutter.alpha2 :as f]))

(def screen
  (f/widget
   (m/Scaffold
    .appBar nil 
    .body
    (m/Center
     .child
     (m/Text "chart")))))
